# Install packages in frontend and backend using npm or yarn.
# Create db and tables (queries are in file - backend.sql);
# Change db user and password in backend/src/db/db.service.ts
# Start server with command - npm run start (server will start on port 3000);
# Start app with command - ng serve (app will start on port 4200);
